<?php
$form = $this->form;
$addUrl = $this->url('zfcadmin/user/student/register', array('query' => array('type' => 'multiple')));
$form->setAttribute('action', $addUrl);
$form->prepare();
echo $this->form($form);